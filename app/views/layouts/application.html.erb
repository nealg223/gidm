<!DOCTYPE html>
<html> 
  <head>
    <title>gidm | <%= @title %></title>
    <%= csrf_meta_tag %> 
    <%= render 'layouts/stylesheets' %>
    <%= javascript_include_tag :defaults %>
  </head>
  <body> 
	<div class="container"> 
	  <%= render 'layouts/header' %> 
	  <section class="round">
	    <% flash.each do |key, value| %>
		  <%= content_tag(:div, value, :class => "flash #{key}") %>
	    <% end %>
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body> 
</html>

<!-- views are really important, use partials, focus on the four key pages, maybe adding a task become an ajax request could take care of inputting tasks as opposed to doing a view for inputting tasks (ajax will come later, last thing you want to do but when you do it will be advantaged by unobtrosive js) -->